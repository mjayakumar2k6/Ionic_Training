import { MyNewPagePipe } from './my-new-page.pipe';

describe('MyNewPagePipe', () => {
  it('create an instance', () => {
    const pipe = new MyNewPagePipe();
    expect(pipe).toBeTruthy();
  });
});
